/**
 * @deprecated Import from `@/lib/data/mock` or use `@/lib/data` accessors instead.
 */
export {
  mockPermits,
  mockOperators,
  mockCounties,
  mockAlerts,
  getMockDashboard,
} from "@/lib/data/mock";
